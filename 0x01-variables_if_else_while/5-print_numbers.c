#include <stdio.h>
/**
* main - Entry point
*
* Return: Always 0 (success)
* print numbers 0-9
*/
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
		printf("%d", num);

	printf("\n");

	return (0);
}
